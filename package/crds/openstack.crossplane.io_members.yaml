---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.21.0
  name: members.openstack.crossplane.io
spec:
  group: openstack.crossplane.io
  names:
    categories:
    - crossplane
    - openstack
    kind: Member
    listKind: MemberList
    plural: members
    singular: member
  scope: Cluster
  versions:
  - additionalPrinterColumns:
    - jsonPath: .status.conditions[?(@.type=='Ready')].status
      name: Ready
      type: string
    - jsonPath: .status.conditions[?(@.type=='Synced')].status
      name: Synced
      type: string
    - jsonPath: .metadata.annotations.crossplane.io/external-name
      name: External Name
      type: string
    - jsonPath: .spec.forProvider.address
      name: Address
      type: string
    - jsonPath: .spec.forProvider.protocolPort
      name: Port
      type: integer
    - jsonPath: .status.atProvider.provisioningStatus
      name: Status
      type: string
    - jsonPath: .metadata.creationTimestamp
      name: Age
      type: date
    name: v1alpha1
    schema:
      openAPIV3Schema:
        description: Member is a managed resource that represents an OpenStack Octavia
          pool member.
        properties:
          apiVersion:
            description: |-
              APIVersion defines the versioned schema of this representation of an object.
              Servers should convert recognized schemas to the latest internal value, and
              may reject unrecognized values.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources
            type: string
          kind:
            description: |-
              Kind is a string value representing the REST resource this object represents.
              Servers may infer this from the endpoint the client submits requests to.
              Cannot be updated.
              In CamelCase.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
            type: string
          metadata:
            type: object
          spec:
            description: MemberSpec defines the desired state of a Member.
            properties:
              deletionPolicy:
                default: Delete
                description: |-
                  DeletionPolicy specifies what will happen to the underlying external
                  when this managed resource is deleted - either "Delete" or "Orphan" the
                  external resource.
                  This field is planned to be deprecated in favor of the ManagementPolicies
                  field in a future release. Currently, both could be set independently and
                  non-default values would be honored if the feature flag is enabled.
                  See the design doc for more information: https://github.com/crossplane/crossplane/blob/499895a25d1a1a0ba1604944ef98ac7a1a71f197/design/design-doc-observe-only-resources.md?plain=1#L223
                enum:
                - Orphan
                - Delete
                type: string
              forProvider:
                description: MemberParameters define the desired state of an OpenStack
                  Octavia pool member.
                properties:
                  address:
                    description: Address is the IP address of the member.
                    type: string
                  adminStateUp:
                    default: true
                    description: AdminStateUp is the administrative state of the member
                      (true = UP).
                    type: boolean
                  backup:
                    default: false
                    description: Backup indicates if the member is a backup member.
                    type: boolean
                  monitorAddress:
                    description: MonitorAddress is an alternate IP address used for
                      health monitoring.
                    type: string
                  monitorPort:
                    description: MonitorPort is an alternate protocol port used for
                      health monitoring.
                    type: integer
                  name:
                    description: Name is the human-readable name for the member.
                    type: string
                  poolId:
                    description: PoolID is the UUID of the pool to which this member
                      belongs.
                    type: string
                  projectId:
                    description: ProjectID is the UUID of the project who owns the
                      member.
                    type: string
                  protocolPort:
                    description: ProtocolPort is the port on which the application
                      is hosted.
                    type: integer
                  subnetId:
                    description: SubnetID is the UUID of the subnet.
                    type: string
                  tags:
                    description: Tags is a set of resource tags.
                    items:
                      type: string
                    type: array
                  weight:
                    description: Weight is the relative portion of traffic this member
                      should receive.
                    type: integer
                required:
                - address
                - poolId
                - protocolPort
                type: object
              managementPolicies:
                default:
                - '*'
                description: |-
                  THIS IS A BETA FIELD. It is on by default but can be opted out
                  through a Crossplane feature flag.
                  ManagementPolicies specify the array of actions Crossplane is allowed to
                  take on the managed and external resources.
                  This field is planned to replace the DeletionPolicy field in a future
                  release. Currently, both could be set independently and non-default
                  values would be honored if the feature flag is enabled. If both are
                  custom, the DeletionPolicy field will be ignored.
                  See the design doc for more information: https://github.com/crossplane/crossplane/blob/499895a25d1a1a0ba1604944ef98ac7a1a71f197/design/design-doc-observe-only-resources.md?plain=1#L223
                  and this one: https://github.com/crossplane/crossplane/blob/444267e84783136daa93568b364a5f01228cacbe/design/one-pager-ignore-changes.md
                items:
                  description: |-
                    A ManagementAction represents an action that the Crossplane controllers
                    can take on an external resource.
                  enum:
                  - Observe
                  - Create
                  - Update
                  - Delete
                  - LateInitialize
                  - '*'
                  type: string
                type: array
              providerConfigRef:
                default:
                  name: default
                description: |-
                  ProviderConfigReference specifies how the provider that will be used to
                  create, observe, update, and delete this managed resource should be
                  configured.
                properties:
                  name:
                    description: Name of the referenced object.
                    type: string
                  policy:
                    description: Policies for referencing.
                    properties:
                      resolution:
                        default: Required
                        description: |-
                          Resolution specifies whether resolution of this reference is required.
                          The default is 'Required', which means the reconcile will fail if the
                          reference cannot be resolved. 'Optional' means this reference will be
                          a no-op if it cannot be resolved.
                        enum:
                        - Required
                        - Optional
                        type: string
                      resolve:
                        description: |-
                          Resolve specifies when this reference should be resolved. The default
                          is 'IfNotPresent', which will attempt to resolve the reference only when
                          the corresponding field is not present. Use 'Always' to resolve the
                          reference on every reconcile.
                        enum:
                        - Always
                        - IfNotPresent
                        type: string
                    type: object
                required:
                - name
                type: object
              writeConnectionSecretToRef:
                description: |-
                  WriteConnectionSecretToReference specifies the namespace and name of a
                  Secret to which any connection details for this managed resource should
                  be written. Connection details frequently include the endpoint, username,
                  and password required to connect to the managed resource.
                properties:
                  name:
                    description: Name of the secret.
                    type: string
                  namespace:
                    description: Namespace of the secret.
                    type: string
                required:
                - name
                - namespace
                type: object
            type: object
          status:
            description: MemberStatus defines the observed state of an OpenStack pool
              member.
            properties:
              atProvider:
                description: MemberProviderStatus defines the observed state of the
                  member at the provider.
                properties:
                  address:
                    description: Address is the IP address of the member.
                    type: string
                  adminStateUp:
                    description: AdminStateUp is the administrative state.
                    type: boolean
                  backup:
                    description: Backup indicates if the member is a backup.
                    type: boolean
                  createdAt:
                    description: CreatedAt is the creation timestamp.
                    type: string
                  memberId:
                    description: MemberID is the unique identifier of the member in
                      OpenStack.
                    type: string
                  monitorAddress:
                    description: MonitorAddress is the alternate health monitor IP.
                    type: string
                  monitorPort:
                    description: MonitorPort is the alternate health monitor port.
                    type: integer
                  name:
                    description: Name is the human-readable name of the member.
                    type: string
                  operatingStatus:
                    description: OperatingStatus is the operating status.
                    type: string
                  poolId:
                    description: PoolID is the pool this member belongs to.
                    type: string
                  projectId:
                    description: ProjectID is the project owner.
                    type: string
                  protocolPort:
                    description: ProtocolPort is the port on which the application
                      is hosted.
                    type: integer
                  provisioningStatus:
                    description: ProvisioningStatus is the provisioning status.
                    type: string
                  subnetId:
                    description: SubnetID is the UUID of the subnet.
                    type: string
                  tags:
                    description: Tags on the member.
                    items:
                      type: string
                    type: array
                  updatedAt:
                    description: UpdatedAt is the last update timestamp.
                    type: string
                  weight:
                    description: Weight is the relative weight of the member.
                    type: integer
                type: object
              conditions:
                description: Conditions of the resource.
                items:
                  description: A Condition that may apply to a resource.
                  properties:
                    lastTransitionTime:
                      description: |-
                        LastTransitionTime is the last time this condition transitioned from one
                        status to another.
                      format: date-time
                      type: string
                    message:
                      description: |-
                        A Message containing details about this condition's last transition from
                        one status to another, if any.
                      type: string
                    observedGeneration:
                      description: |-
                        ObservedGeneration represents the .metadata.generation that the condition was set based upon.
                        For instance, if .metadata.generation is currently 12, but the .status.conditions[x].observedGeneration is 9, the condition is out of date
                        with respect to the current state of the instance.
                      format: int64
                      type: integer
                    reason:
                      description: A Reason for this condition's last transition from
                        one status to another.
                      type: string
                    status:
                      description: Status of this condition; is it currently True,
                        False, or Unknown?
                      type: string
                    type:
                      description: |-
                        Type of this condition. At most one of each condition type may apply to
                        a resource at any point in time.
                      type: string
                  required:
                  - lastTransitionTime
                  - reason
                  - status
                  - type
                  type: object
                type: array
                x-kubernetes-list-map-keys:
                - type
                x-kubernetes-list-type: map
            type: object
        required:
        - spec
        type: object
    served: true
    storage: true
    subresources:
      status: {}
