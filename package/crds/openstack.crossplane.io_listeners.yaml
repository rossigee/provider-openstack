---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.21.0
  name: listeners.openstack.crossplane.io
spec:
  group: openstack.crossplane.io
  names:
    categories:
    - crossplane
    - openstack
    kind: Listener
    listKind: ListenerList
    plural: listeners
    singular: listener
  scope: Cluster
  versions:
  - additionalPrinterColumns:
    - jsonPath: .status.conditions[?(@.type=='Ready')].status
      name: Ready
      type: string
    - jsonPath: .status.conditions[?(@.type=='Synced')].status
      name: Synced
      type: string
    - jsonPath: .metadata.annotations.crossplane.io/external-name
      name: External Name
      type: string
    - jsonPath: .spec.forProvider.protocol
      name: Protocol
      type: string
    - jsonPath: .spec.forProvider.protocolPort
      name: Port
      type: integer
    - jsonPath: .status.atProvider.provisioningStatus
      name: Status
      type: string
    - jsonPath: .metadata.creationTimestamp
      name: Age
      type: date
    name: v1alpha1
    schema:
      openAPIV3Schema:
        description: Listener is a managed resource that represents an OpenStack Octavia
          listener.
        properties:
          apiVersion:
            description: |-
              APIVersion defines the versioned schema of this representation of an object.
              Servers should convert recognized schemas to the latest internal value, and
              may reject unrecognized values.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources
            type: string
          kind:
            description: |-
              Kind is a string value representing the REST resource this object represents.
              Servers may infer this from the endpoint the client submits requests to.
              Cannot be updated.
              In CamelCase.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
            type: string
          metadata:
            type: object
          spec:
            description: ListenerSpec defines the desired state of a Listener.
            properties:
              deletionPolicy:
                default: Delete
                description: |-
                  DeletionPolicy specifies what will happen to the underlying external
                  when this managed resource is deleted - either "Delete" or "Orphan" the
                  external resource.
                  This field is planned to be deprecated in favor of the ManagementPolicies
                  field in a future release. Currently, both could be set independently and
                  non-default values would be honored if the feature flag is enabled.
                  See the design doc for more information: https://github.com/crossplane/crossplane/blob/499895a25d1a1a0ba1604944ef98ac7a1a71f197/design/design-doc-observe-only-resources.md?plain=1#L223
                enum:
                - Orphan
                - Delete
                type: string
              forProvider:
                description: ListenerParameters define the desired state of an OpenStack
                  Octavia listener.
                properties:
                  adminStateUp:
                    default: true
                    description: AdminStateUp is the administrative state of the listener
                      (true = UP).
                    type: boolean
                  allowedCidrs:
                    description: AllowedCIDRs is a list of IPv4/IPv6 CIDRs.
                    items:
                      type: string
                    type: array
                  connLimit:
                    description: ConnLimit is the maximum number of connections allowed.
                      Default is -1 (no limit).
                    type: integer
                  defaultPoolId:
                    description: DefaultPoolID is the ID of the default pool with
                      which the listener is associated.
                    type: string
                  defaultTlsContainerRef:
                    description: DefaultTlsContainerRef is a reference to a Barbican
                      container of TLS secrets.
                    type: string
                  description:
                    description: Description of the listener.
                    type: string
                  insertHeaders:
                    additionalProperties:
                      type: string
                    description: InsertHeaders is a dictionary of optional headers
                      to insert into the request.
                    type: object
                  loadbalancerId:
                    description: LoadbalancerID is the UUID of the load balancer on
                      which to provision this listener.
                    type: string
                  name:
                    description: Name is the human-readable name for the listener.
                    type: string
                  projectId:
                    description: ProjectID is the UUID of the project who owns the
                      listener.
                    type: string
                  protocol:
                    description: Protocol is the protocol - TCP, UDP, HTTP, HTTPS,
                      or TERMINATED_HTTPS.
                    type: string
                  protocolPort:
                    description: ProtocolPort is the port on which to listen for client
                      traffic.
                    type: integer
                  sniContainerRefs:
                    description: SniContainerRefs is a list of references to TLS secrets
                      for SNI.
                    items:
                      type: string
                    type: array
                  tags:
                    description: Tags is a set of resource tags.
                    items:
                      type: string
                    type: array
                  timeoutClientData:
                    description: TimeoutClientData is the frontend client inactivity
                      timeout in milliseconds.
                    type: integer
                  timeoutMemberConnect:
                    description: TimeoutMemberConnect is the backend member connection
                      timeout in milliseconds.
                    type: integer
                  timeoutMemberData:
                    description: TimeoutMemberData is the backend member inactivity
                      timeout in milliseconds.
                    type: integer
                  timeoutTcpInspect:
                    description: TimeoutTCPInspect is the time in milliseconds to
                      wait for additional TCP packets.
                    type: integer
                required:
                - loadbalancerId
                - protocol
                - protocolPort
                type: object
              managementPolicies:
                default:
                - '*'
                description: |-
                  THIS IS A BETA FIELD. It is on by default but can be opted out
                  through a Crossplane feature flag.
                  ManagementPolicies specify the array of actions Crossplane is allowed to
                  take on the managed and external resources.
                  This field is planned to replace the DeletionPolicy field in a future
                  release. Currently, both could be set independently and non-default
                  values would be honored if the feature flag is enabled. If both are
                  custom, the DeletionPolicy field will be ignored.
                  See the design doc for more information: https://github.com/crossplane/crossplane/blob/499895a25d1a1a0ba1604944ef98ac7a1a71f197/design/design-doc-observe-only-resources.md?plain=1#L223
                  and this one: https://github.com/crossplane/crossplane/blob/444267e84783136daa93568b364a5f01228cacbe/design/one-pager-ignore-changes.md
                items:
                  description: |-
                    A ManagementAction represents an action that the Crossplane controllers
                    can take on an external resource.
                  enum:
                  - Observe
                  - Create
                  - Update
                  - Delete
                  - LateInitialize
                  - '*'
                  type: string
                type: array
              providerConfigRef:
                default:
                  name: default
                description: |-
                  ProviderConfigReference specifies how the provider that will be used to
                  create, observe, update, and delete this managed resource should be
                  configured.
                properties:
                  name:
                    description: Name of the referenced object.
                    type: string
                  policy:
                    description: Policies for referencing.
                    properties:
                      resolution:
                        default: Required
                        description: |-
                          Resolution specifies whether resolution of this reference is required.
                          The default is 'Required', which means the reconcile will fail if the
                          reference cannot be resolved. 'Optional' means this reference will be
                          a no-op if it cannot be resolved.
                        enum:
                        - Required
                        - Optional
                        type: string
                      resolve:
                        description: |-
                          Resolve specifies when this reference should be resolved. The default
                          is 'IfNotPresent', which will attempt to resolve the reference only when
                          the corresponding field is not present. Use 'Always' to resolve the
                          reference on every reconcile.
                        enum:
                        - Always
                        - IfNotPresent
                        type: string
                    type: object
                required:
                - name
                type: object
              writeConnectionSecretToRef:
                description: |-
                  WriteConnectionSecretToReference specifies the namespace and name of a
                  Secret to which any connection details for this managed resource should
                  be written. Connection details frequently include the endpoint, username,
                  and password required to connect to the managed resource.
                properties:
                  name:
                    description: Name of the secret.
                    type: string
                  namespace:
                    description: Namespace of the secret.
                    type: string
                required:
                - name
                - namespace
                type: object
            type: object
          status:
            description: ListenerStatus defines the observed state of an OpenStack
              listener.
            properties:
              atProvider:
                description: ListenerProviderStatus defines the observed state of
                  the listener at the provider.
                properties:
                  adminStateUp:
                    description: AdminStateUp is the administrative state.
                    type: boolean
                  connLimit:
                    description: ConnLimit is the connection limit.
                    type: integer
                  createdAt:
                    description: CreatedAt is the creation timestamp.
                    type: string
                  defaultPoolId:
                    description: DefaultPoolID is the default pool ID.
                    type: string
                  description:
                    description: Description of the listener.
                    type: string
                  listenerId:
                    description: ListenerID is the unique identifier of the listener
                      in OpenStack.
                    type: string
                  loadbalancerId:
                    description: LoadbalancerID is the load balancer this listener
                      belongs to.
                    type: string
                  name:
                    description: Name is the human-readable name of the listener.
                    type: string
                  operatingStatus:
                    description: OperatingStatus is the operating status.
                    type: string
                  projectId:
                    description: ProjectID is the project owner.
                    type: string
                  protocol:
                    description: Protocol is the listener protocol.
                    type: string
                  protocolPort:
                    description: ProtocolPort is the listener port.
                    type: integer
                  provisioningStatus:
                    description: ProvisioningStatus is the provisioning status.
                    type: string
                  tags:
                    description: Tags on the listener.
                    items:
                      type: string
                    type: array
                  updatedAt:
                    description: UpdatedAt is the last update timestamp.
                    type: string
                type: object
              conditions:
                description: Conditions of the resource.
                items:
                  description: A Condition that may apply to a resource.
                  properties:
                    lastTransitionTime:
                      description: |-
                        LastTransitionTime is the last time this condition transitioned from one
                        status to another.
                      format: date-time
                      type: string
                    message:
                      description: |-
                        A Message containing details about this condition's last transition from
                        one status to another, if any.
                      type: string
                    observedGeneration:
                      description: |-
                        ObservedGeneration represents the .metadata.generation that the condition was set based upon.
                        For instance, if .metadata.generation is currently 12, but the .status.conditions[x].observedGeneration is 9, the condition is out of date
                        with respect to the current state of the instance.
                      format: int64
                      type: integer
                    reason:
                      description: A Reason for this condition's last transition from
                        one status to another.
                      type: string
                    status:
                      description: Status of this condition; is it currently True,
                        False, or Unknown?
                      type: string
                    type:
                      description: |-
                        Type of this condition. At most one of each condition type may apply to
                        a resource at any point in time.
                      type: string
                  required:
                  - lastTransitionTime
                  - reason
                  - status
                  - type
                  type: object
                type: array
                x-kubernetes-list-map-keys:
                - type
                x-kubernetes-list-type: map
            type: object
        required:
        - spec
        type: object
    served: true
    storage: true
    subresources:
      status: {}
