apiVersion: identity.openstack.m.crossplane.io/v1beta1
kind: Project
metadata:
  name: example-project
spec:
  forProvider:
    name: my-project
    description: "Example OpenStack project"
    enabled: true
  providerConfigRef:
    name: default
  deletionPolicy: Delete
