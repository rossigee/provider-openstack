# Standardized Release Template for Crossplane Providers
# Version: 2025-09-21
#
# This template handles ALL publishing for releases
# - Builds from source on tag creation
# - Creates both version-specific and latest tags from SAME image
# - Publishes to primary registry (ghcr.io/rossigee)
# - Creates GitHub release with notes

name: Release

on:
  push:
    tags:
      - 'v*'

env:
  GO_VERSION: '1.25.1'

jobs:
  release:
    runs-on: ubuntu-24.04
    permissions:
      contents: write
      packages: write
      id-token: write

    steps:
      - name: Checkout
        uses: actions/checkout@v4
        with:
          fetch-depth: 0
          submodules: true

      - name: Setup Go
        uses: actions/setup-go@v5
        with:
          go-version: ${{ env.GO_VERSION }}

      - name: Vendor Dependencies
        run: go mod vendor

      - name: Extract Version
        id: version
        run: echo "version=${GITHUB_REF#refs/tags/}" >> $GITHUB_OUTPUT

      - name: Set up Docker Buildx
        uses: docker/setup-buildx-action@v3

      - name: Login to GitHub Container Registry
        uses: docker/login-action@v3
        with:
          registry: ghcr.io
          username: ${{ github.actor }}
          password: ${{ secrets.PAT_TOKEN }}

      - name: Build
        run: make build
        env:
          VERSION: ${{ steps.version.outputs.version }}

      - name: Verify Build Artifacts
        run: |
          # Verify provider binary exists (build system uses _output/bin/)
          ls -la _output/bin/
          ls -la _output/bin/linux_amd64/
          if [ ! -f _output/bin/linux_amd64/provider ]; then
            echo "ERROR: Provider binary not found!"
            find . -name "provider" -type f 2>/dev/null || echo "No provider binary found anywhere"
            exit 1
          fi
          ls -la _output/bin/linux_amd64/provider
          file _output/bin/linux_amd64/provider
          echo "✅ Provider binary verification successful"

      # CRITICAL: Build Docker image once, tag it with both version and latest
      - name: Build Docker Image (Version)
        run: |
          # Create temporary build context and copy binary (matching make docker build)
          BUILD_CONTEXT=$(mktemp -d)
          cp cluster/images/provider-openstack/Dockerfile $BUILD_CONTEXT/
          cp -r _output/bin/ $BUILD_CONTEXT/

          # Build container image for specific version with OpenContainers labels
          docker build -t ghcr.io/rossigee/provider-openstack:${{ steps.version.outputs.version }} \
            --build-arg VERSION=${{ steps.version.outputs.version }} \
            --build-arg TARGETOS=linux \
            --build-arg TARGETARCH=amd64 \
            --label "org.opencontainers.image.title=provider-openstack" \
            --label "org.opencontainers.image.description=Crossplane provider for OpenStack cloud resources" \
            --label "org.opencontainers.image.version=${{ steps.version.outputs.version }}" \
            --label "org.opencontainers.image.revision=${{ github.sha }}" \
            --label "org.opencontainers.image.created=$(date -u +'%Y-%m-%dT%H:%M:%SZ')" \
            --label "org.opencontainers.image.source=${{ github.server_url }}/${{ github.repository }}" \
            --label "org.opencontainers.image.url=${{ github.server_url }}/${{ github.repository }}" \
            --label "org.opencontainers.image.documentation=${{ github.server_url }}/${{ github.repository }}/blob/${{ steps.version.outputs.version }}/README.md" \
            --label "org.opencontainers.image.vendor=rossigee" \
            --label "org.opencontainers.image.licenses=Apache-2.0" \
            --label "org.opencontainers.image.ref.name=${{ steps.version.outputs.version }}" \
            $BUILD_CONTEXT

          # Cleanup
          rm -rf $BUILD_CONTEXT

      # CRITICAL: Tag the SAME image as latest (ensures identical images)
      - name: Build Docker Image (Latest)
        run: |
          # Tag the same image as latest
          docker tag ghcr.io/rossigee/provider-openstack:${{ steps.version.outputs.version }} \
            ghcr.io/rossigee/provider-openstack:latest

      # CRITICAL: Push both tags (they point to the same image)
      - name: Push Docker Images
        run: |
          # Push both version and latest tags
          docker push ghcr.io/rossigee/provider-openstack:${{ steps.version.outputs.version }}
          docker push ghcr.io/rossigee/provider-openstack:latest

      - name: Publish Crossplane Packages
        run: |
          # Publish versioned package
          make publish REGISTRY_ORGS="ghcr.io/rossigee" XPKG_REG_ORGS="ghcr.io/rossigee"
          # Publish latest package
          make publish VERSION=latest REGISTRY_ORGS="ghcr.io/rossigee" XPKG_REG_ORGS="ghcr.io/rossigee"
        env:
          VERSION: ${{ steps.version.outputs.version }}

      - name: Create GitHub Release
        uses: softprops/action-gh-release@v2
        with:
          name: Release ${{ steps.version.outputs.version }}
          draft: false
          prerelease: false
          generate_release_notes: true